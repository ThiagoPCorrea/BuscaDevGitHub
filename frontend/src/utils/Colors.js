import { createTheme } from '@mui/material/styles';

export const ButtonTheme = createTheme({
  status: {
    danger: '#e53e3e',
  },
  palette: {
    gray: {
      main: '#47525E',
      contrastText: '#fff',
    },
  },
});